import "./style.css";

const Form = () => (
    <form className="list__field">
            <input className="form__field" placeholder="Co jest do kupienia?"/>
            <button className="button">Dodaj do listy</button>
            
        </form>
)

export default Form;